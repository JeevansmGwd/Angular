import { TwoCharPipePipe } from './two-char-pipe.pipe';

describe('TwoCharPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TwoCharPipePipe();
    expect(pipe).toBeTruthy();
  });
});
